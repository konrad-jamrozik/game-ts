/**
 * See docs/setup/about_tsconfig.md for context on this file.
 */
{
  // Applicability of this config.
  // --------------------
  "include": ["vite.config.ts", "vitest.config.ts", "test/utils/unusedNodeTestUtils.ts", "scripts/**/*.ts"],

  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "lib": ["ES2024"],
    "types": ["node"] // Node.js types, e.g. for `process`.
  }
}
